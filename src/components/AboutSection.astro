---

---

<section class="py-24 bg-white relative" id="about">
  <div class="container mx-auto px-6">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
      <div class="about-content">
        <h2 class="text-5xl md:text-6xl font-serif font-bold text-coffee-900 mb-6">
          Where <span class="text-gradient">Design</span> Meets Coffee
        </h2>
        <p class="text-lg text-coffee-700 mb-8 leading-relaxed">
          Designers' Cafe is a creative sanctuary in Taguig where coffee culture and design inspiration blend together. Founded by passionate creatives, our space is crafted for thinkers, makers, and dreamers to connect, collaborate, and enjoy specialty drinks in a welcoming, inspiring atmosphere.
        </p>
        <!-- Stats -->
        <div class="grid grid-cols-3 gap-6 text-center mb-8">
          <div class="stat-item" style="animation-delay:0.1s">
            <div class="text-3xl font-bold text-pencil mb-2" data-count="500">0</div>
            <p class="text-coffee-600 text-sm">Creative Minds Served</p>
          </div>
          <div class="stat-item" style="animation-delay:0.2s">
            <div class="text-3xl font-bold text-pencil mb-2" data-count="15">0</div>
            <p class="text-coffee-600 text-sm">Signature Drinks</p>
          </div>
          <div class="stat-item" style="animation-delay:0.3s">
            <div class="text-3xl font-bold text-pencil mb-2" data-count="2">0</div>
            <p class="text-coffee-600 text-sm">Years Brewing</p>
          </div>
        </div>
      </div>
      <div class="about-image flex justify-center" style="animation-delay:0.4s">
        <img 
          src="/assets/pembo2.jpg" 
          alt="Signature coffee at Designers' Cafe" 
          class="rounded-2xl shadow-lg object-cover w-full max-w-xs h-[480px] md:h-[600px]" 
          style="background: #f8f4f0;"
        />
      </div>
    </div>
  </div>
</section>
<style>
  @keyframes fade-in-left {
    from { opacity: 0; transform: translateX(-40px);}
    to { opacity: 1; transform: translateX(0);}
  }
  @keyframes fade-in-right {
    from { opacity: 0; transform: translateX(40px);}
    to { opacity: 1; transform: translateX(0);}
  }
  @keyframes fade-in-up {
    from { opacity: 0; transform: translateY(32px);}
    to { opacity: 1; transform: translateY(0);}
  }
  .animate-fade-in-left {
    animation: fade-in-left 0.8s cubic-bezier(.4,0,.2,1) both;
  }
  .animate-fade-in-right {
    animation: fade-in-right 0.8s cubic-bezier(.4,0,.2,1) both;
  }
  .animate-fade-in-up {
    animation: fade-in-up 0.8s cubic-bezier(.4,0,.2,1) both;
  }
</style>
<script>
  // GSAP animations will be handled by main.js
  // This ensures consistency and better performance
</script>
