---
const bgImage = "/public/assets/midnightbg.jpg"; // adjust path if needed
---

<section
  class="min-h-screen flex items-center justify-center relative overflow-hidden pt-20"
  id="hero"
  aria-label="Designers' Cafe Hero Section"
  style={`background: url('${bgImage}') center center / cover no-repeat; background-color: var(--coffee-900, #2E1C09);`}
>
  <!-- Animated floating shapes -->
  <div class="absolute inset-0 opacity-10 z-10">
    <div class="absolute top-20 left-10 w-32 h-32 rounded-full animate-float" style="background: var(--pencil, #D4943A); animation-delay: 0s;"></div>
    <div class="absolute bottom-32 right-20 w-24 h-24 rounded-full animate-float" style="background: var(--coffee-400, #D4943A); animation-delay: 2s;"></div>
    <div class="absolute top-1/2 left-1/4 w-16 h-16 rounded-full animate-float" style="background: var(--coffee-600, #8B4B1A); animation-delay: 4s;"></div>
    <!-- Unique animated gradient ring -->
    <div class="absolute top-1/3 right-1/3 w-40 h-40 rounded-full border-4 border-[var(--pencil)] animate-spin-slow"></div>
  </div>
  
  <div class="container mx-auto px-6 text-center relative z-20">
    <div class="max-w-4xl mx-auto">
      <!-- Animated tagline -->
      <p class="font-medium mb-4 animate-fade-in" id="tagline" style="color: var(--cream, #F8F4F0); font-family: var(--font-sans);">
        Where creativity meets caffeine
      </p>
      
      <!-- Main heading with split text animation -->
      <h1 class="text-5xl md:text-7xl font-serif font-bold mb-6 drop-shadow-lg animate-slide-in-down" style="color: var(--cream, #F8F4F0); font-family: var(--font-serif);">
        <span class="block animate-slide-in-up" id="hero-text-1" style="color: var(--coffee-50, #FDF7F0);">Brewing</span>
        <span class="block text-gradient animate-slide-in-up" id="hero-text-2" style="color: var(--pencil, #D4943A);">Connections</span>
        <span class="block text-sm md:text-lg font-sans font-normal mt-4 animate-fade-in" id="hero-text-3" style="color: var(--cream, #F8F4F0); font-family: var(--font-sans);">One cup at a time</span>
      </h1>
      
      <p class="text-lg md:text-xl max-w-2xl mx-auto mb-8 drop-shadow animate-fade-in" id="hero-description" style="color: var(--cream, #F8F4F0); font-family: var(--font-sans);">
        Experience the perfect blend of creativity and coffee at Designers' Cafe. 
        Where every sip fuels inspiration and every moment sparks connections.
      </p>
      
      <!-- CTA Buttons with stagger animation -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center animate-fade-in" id="hero-buttons" style="animation-delay: 0.3s;">
        <button class="cta-primary px-8 py-4 rounded-full font-semibold hover:bg-[var(--coffee-600)] transition-all duration-300 transform hover:scale-105 animate-pulse-glow focus:outline-none focus:ring-4 focus:ring-[var(--pencil)] focus:ring-opacity-50"
          style="background: var(--pencil, #D4943A); color: var(--cream, #F8F4F0); font-family: var(--font-sans);">
          Explore Menu
        </button>
        <button class="cta-secondary border-2 px-8 py-4 rounded-full font-semibold hover:bg-[var(--pencil)] hover:text-white transition-all duration-300 focus:outline-none focus:ring-4 focus:ring-[var(--pencil)] focus:ring-opacity-50"
          style="border-color: var(--pencil, #D4943A); color: var(--pencil, #D4943A); font-family: var(--font-sans);">
          Visit Us Today
        </button>
      </div>
      
      <!-- Hours indicator -->
      <div class="mt-12 animate-fade-in" id="hours-indicator" style="animation-delay: 0.5s;">
        <div class="inline-flex items-center glass-morphism px-6 py-3 rounded-full" style="background: rgba(255,255,255,0.08);">
          <div class="w-3 h-3 bg-green-500 rounded-full mr-3 animate-pulse"></div>
          <span class="font-medium" style="color: var(--cream, #F8F4F0); font-family: var(--font-sans);">Open Today: 3:00 PM - 11:00 PM</span>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-fade-in z-20" id="scroll-indicator" style="animation-delay: 0.7s;">
    <div class="flex flex-col items-center">
      <span class="text-white text-sm mb-2 drop-shadow" style="font-family: var(--font-sans);">Scroll to explore</span>
      <div class="w-6 h-10 border-2 rounded-full flex justify-center" style="border-color: var(--coffee-400, #D4943A);">
        <div class="w-1 h-3 rounded-full mt-2 animate-bounce" style="background: var(--coffee-400, #D4943A);"></div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in {
    from { opacity: 0; transform: translateY(24px);}
    to { opacity: 1; transform: translateY(0);}
  }
  @keyframes slide-in-down {
    from { opacity: 0; transform: translateY(-48px);}
    to { opacity: 1; transform: translateY(0);}
  }
  @keyframes slide-in-up {
    from { opacity: 0; transform: translateY(48px);}
    to { opacity: 1; transform: translateY(0);}
  }
  @keyframes spin-slow {
    0% { transform: rotate(0deg);}
    100% { transform: rotate(360deg);}
  }
  .animate-fade-in {
    animation: fade-in 0.8s cubic-bezier(.4,0,.2,1) both;
  }
  .animate-slide-in-down {
    animation: slide-in-down 1s cubic-bezier(.4,0,.2,1) both;
  }
  .animate-slide-in-up {
    animation: slide-in-up 1s cubic-bezier(.4,0,.2,1) both;
  }
  .animate-spin-slow {
    animation: spin-slow 8s linear infinite;
    border-image: linear-gradient(90deg, var(--pencil, #D4943A), var(--coffee-400, #D49443