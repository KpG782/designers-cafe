---
---

<section class="py-20 relative overflow-hidden" id="contact" style="background: linear-gradient(135deg, var(--coffee-900) 0%, var(--coffee-800) 100%);">
  <!-- Coffee steam background animations -->
  <div class="absolute inset-0 opacity-10 pointer-events-none">
    <div class="steam-line steam-1"></div>
    <div class="steam-line steam-2"></div>
    <div class="steam-line steam-3"></div>
    <div class="steam-line steam-4"></div>
    <div class="floating-bean bean-1"></div>
    <div class="floating-bean bean-2"></div>
    <div class="floating-bean bean-3"></div>
  </div>

  <div class="container mx-auto px-6 relative z-10">
    <!-- Section Header -->
    <div class="text-center mb-16 contact-header" style="opacity: 0;">
      <div class="coffee-cup-floating">
        <svg viewBox="0 0 24 24" fill="currentColor" class="w-16 h-16 mx-auto mb-6" style="color: var(--pencil);">
          <path d="M18.5 3H6C4.89 3 4 3.89 4 5V19C4 20.11 4.89 21 6 21H18C19.11 21 20 20.11 20 19V5C20 3.89 19.11 3 18.5 3ZM12 7C10.34 7 9 8.34 9 10S10.34 13 12 13 15 11.66 15 10 13.66 7 12 7Z"/>
        </svg>
      </div>
      <h2 class="text-4xl md:text-6xl font-serif font-bold mb-6" style="color: var(--cream); font-family: var(--font-serif);">
        Let's <span style="color: var(--pencil);">Brew</span> Together
      </h2>
      <p class="text-lg md:text-xl max-w-2xl mx-auto mb-8" style="color: var(--cream); font-family: var(--font-sans); opacity: 0.9;">
        Have questions, feedback, or want to collaborate? We'd love to hear from you! 
        Reach out and let's create something amazing together.
      </p>
      <div class="w-24 h-1 mx-auto rounded-full" style="background: var(--pencil);"></div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">
      <!-- Contact Form -->
      <div class="contact-form coffee-card" style="opacity: 0;">
        <div class="card-header">
          <div class="form-icon">
            <svg viewBox="0 0 24 24" fill="currentColor" class="w-8 h-8">
              <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
            </svg>
          </div>
          <h3 class="text-2xl font-serif font-bold mb-2" style="color: var(--cream); font-family: var(--font-serif);">
            Send us a Message
          </h3>
          <p class="text-sm mb-6" style="color: var(--cream); opacity: 0.8;">Fill out the form and we'll get back to you</p>
        </div>

        <form class="space-y-6" action="#" method="POST" role="form" aria-label="Contact form" id="contact-form">
          <div class="form-group" style="opacity: 0;">
            <label for="name" class="form-label">Full Name *</label>
            <div class="input-container">
              <input 
                type="text" 
                id="name" 
                name="name" 
                required 
                class="form-input"
                placeholder="Your full name"
                aria-required="true"
              />
              <div class="input-icon">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                </svg>
              </div>
            </div>
          </div>

          <div class="form-group" style="opacity: 0;">
            <label for="email" class="form-label">Email Address *</label>
            <div class="input-container">
              <input 
                type="email" 
                id="email" 
                name="email" 
                required 
                class="form-input"
                placeholder="your.email@example.com"
                aria-required="true"
              />
              <div class="input-icon">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-1.997 1.884z"/>
                </svg>
              </div>
            </div>
          </div>

          <div class="form-group" style="opacity: 0;">
            <label for="phone" class="form-label">Phone Number</label>
            <div class="input-container">
              <input 
                type="tel" 
                id="phone" 
                name="phone"
                class="form-input"
                placeholder="+63 XXX XXX XXXX"
              />
              <div class="input-icon">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                </svg>
              </div>
            </div>
          </div>

          <div class="form-group" style="opacity: 0;">
            <label for="subject" class="form-label">Subject *</label>
            <div class="input-container">
              <select 
                id="subject" 
                name="subject" 
                required
                class="form-input form-select"
                aria-required="true"
              >
                <option value="">Select a topic</option>
                <option value="general">☕ General Inquiry</option>
                <option value="catering">🍰 Catering Services</option>
                <option value="events">🎉 Private Events</option>
                <option value="feedback">💭 Feedback</option>
                <option value="collaboration">🤝 Collaboration</option>
                <option value="other">📝 Other</option>
              </select>
              <div class="input-icon">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"/>
                </svg>
              </div>
            </div>
          </div>

          <div class="form-group" style="opacity: 0;">
            <label for="message" class="form-label">Message *</label>
            <div class="input-container">
              <textarea 
                id="message" 
                name="message" 
                required 
                rows="6"
                class="form-input form-textarea"
                placeholder="Tell us about your inquiry, ideas, or how we can help you..."
                aria-required="true"
              ></textarea>
              <div class="input-icon textarea-icon">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                </svg>
              </div>
            </div>
          </div>

          <div class="form-group" style="opacity: 0;">
            <button 
              type="submit" 
              class="submit-btn"
              aria-label="Send your message to Designers' Cafe"
            >
              <span class="btn-text">Send Message</span>
              <div class="btn-icon">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
                </svg>
              </div>
              <div class="btn-loading" style="display: none;">
                <svg class="w-5 h-5 animate-spin" fill="none" viewBox="0 0 24 24">
                  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                  <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
              </div>
            </button>
          </div>
        </form>
      </div>

      <!-- Contact Info and Buy Me Coffee -->
      <div class="space-y-8">
        <!-- Contact Information -->
        <div class="contact-info coffee-card" style="opacity: 0;">
          <div class="card-header">
            <div class="form-icon">
              <svg viewBox="0 0 24 24" fill="currentColor" class="w-8 h-8">
                <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/>
              </svg>
            </div>
            <h3 class="text-2xl font-serif font-bold mb-2" style="color: var(--cream); font-family: var(--font-serif);">
              Visit Our Cafe
            </h3>
            <p class="text-sm mb-6" style="color: var(--cream); opacity: 0.8;">Come experience our creative atmosphere</p>
          </div>

          <div class="contact-details space-y-4">
            <div class="contact-item" style="opacity: 0;">
              <div class="contact-icon">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z"/>
                </svg>
              </div>
              <div>
                <h4 class="font-semibold mb-1" style="color: var(--cream);">Call Us</h4>
                <a href="tel:+639615235176" class="contact-link">+63 961 523 5176</a>
              </div>
            </div>

            <div class="contact-item" style="opacity: 0;">
              <div class="contact-icon">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"/>
                  <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"/>
                </svg>
              </div>
              <div>
                <h4 class="font-semibold mb-1" style="color: var(--cream);">Email Us</h4>
                <a href="mailto:designerscafeshop@gmail.com" class="contact-link">designerscafeshop@gmail.com</a>
              </div>
            </div>

            <div class="contact-item" style="opacity: 0;">
              <div class="contact-icon">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"/>
                </svg>
              </div>
              <div>
                <h4 class="font-semibold mb-1" style="color: var(--cream);">Location</h4>
                <p class="contact-link">Pembo, Taguig City</p>
              </div>
            </div>

            <div class="contact-item" style="opacity: 0;">
              <div class="contact-icon">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
                </svg>
              </div>
              <div>
                <h4 class="font-semibold mb-1" style="color: var(--cream);">Hours</h4>
                <p class="contact-link">Sun-Thu: 3PM-11PM</p>
                <p class="text-red-400 text-sm">Friday: Closed</p>
              </div>
            </div>
          </div>

          <!-- Social Media -->
          <div class="social-section" style="opacity: 0;">
            <h4 class="font-semibold text-lg mb-4 text-center" style="color: var(--cream);">Follow Our Journey</h4>
            <div class="flex justify-center space-x-4">
              <a href="https://www.facebook.com/DesignersCafeShop" target="_blank" class="social-link facebook">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                </svg>
              </a>
              <a href="https://www.instagram.com/designerscafeshop" target="_blank" class="social-link instagram">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                </svg>
              </a>
              <a href="https://twitter.com/designerscafe" target="_blank" class="social-link twitter">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M23.953 4.57a10 10 0 01-2.825.775 4.958 4.958 0 002.163-2.723c-.951.555-2.005.959-3.127 1.184a4.92 4.92 0 00-8.384 4.482C7.69 8.095 4.067 6.13 1.64 3.162a4.822 4.822 0 00-.666 2.475c0 1.71.87 3.213 2.188 4.096a4.904 4.904 0 01-2.228-.616v.06a4.923 4.923 0 003.946 4.827 4.996 4.996 0 01-2.212.085 4.936 4.936 0 004.604 3.417 9.867 9.867 0 01-6.102 2.105c-.39 0-.779-.023-1.17-.067a13.995 13.995 0 007.557 2.209c9.053 0 13.998-7.496 13.998-13.985 0-.21 0-.42-.015-.63A9.935 9.935 0 0024 4.59z"/>
                </svg>
              </a>
            </div>
          </div>
        </div>

        <!-- Buy Me Coffee Section -->
        <div class="buy-coffee-section coffee-card special-card" style="opacity: 0;">
          <div class="coffee-heart-container">
            <div class="coffee-heart">
              <svg class="w-12 h-12" fill="currentColor" viewBox="0 0 24 24">
                <path d="M20.216 6.415l-.132-.666c-.119-.598-.388-1.163-.766-1.664a4.993 4.993 0 0 0-1.353-1.216c-.669-.414-1.418-.75-2.131-.75-.52 0-1.04.133-1.543.398-.264.14-.519.315-.766.515L12 4.352 10.475 3.031c-.247-.2-.502-.375-.766-.515A3.693 3.693 0 0 0 8.166 2c-.713 0-1.462.336-2.131.75a4.993 4.993 0 0 0-1.353 1.216 4.036 4.036 0 0 0-.766 1.664l-.132.666C3.629 7.51 3.5 8.621 3.5 9.77c0 4.247 2.28 7.311 4.575 9.355 1.324 1.179 2.735 2.128 3.925 2.875 1.19-.747 2.601-1.696 3.925-2.875C18.22 17.081 20.5 14.017 20.5 9.77c0-1.149-.129-2.26-.284-3.355z"/>
              </svg>
            </div>
          </div>
          <h3 class="text-2xl font-bold mb-3 text-center" style="color: var(--pencil); font-family: var(--font-serif);">
            Love This Website?
          </h3>
          <p class="text-base text-center mb-6" style="color: var(--cream); line-height: 1.6;">
            This website was lovingly crafted by a passionate developer.<br>
            <span style="color: var(--pencil); font-weight: 600;">Support the creator and fuel more projects!</span>
          </p>
          <a href="https://buymeacoffee.com/Loki123" 
             target="_blank" 
             rel="noopener" 
             class="coffee-btn">
            <span class="btn-coffee-icon">☕</span>
            <span class="btn-coffee-text">Buy Me a Coffee</span>
            <div class="btn-coffee-ripple"></div>
          </a>
          <p class="text-sm mt-4 text-center" style="color: var(--coffee-200);">
            Every coffee helps create more amazing experiences! ✨
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Background animations */
  @keyframes steam-rise {
    0% { transform: translateY(0) scale(1); opacity: 0; }
    20% { opacity: 1; }
    80% { opacity: 1; }
    100% { transform: translateY(-60px) scale(0.8); opacity: 0; }
  }

  @keyframes float-bean {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    33% { transform: translateY(-20px) rotate(120deg); }
    66% { transform: translateY(10px) rotate(240deg); }
  }

  .steam-line {
    position: absolute;
    width: 3px;
    height: 40px;
    background: linear-gradient(180deg, var(--pencil) 0%, transparent 100%);
    border-radius: 50%;
    animation: steam-rise 4s ease-in-out infinite;
  }

  .steam-1 { top: 20%; left: 20%; animation-delay: 0s; }
  .steam-2 { top: 40%; right: 25%; animation-delay: 1s; }
  .steam-3 { bottom: 30%; left: 30%; animation-delay: 2s; }
  .steam-4 { top: 60%; right: 15%; animation-delay: 3s; }

  .floating-bean {
    position: absolute;
    width: 20px;
    height: 20px;
    background: var(--pencil);
    border-radius: 50%;
    animation: float-bean 8s ease-in-out infinite;
  }

  .bean-1 { top: 15%; left: 80%; animation-delay: 0s; }
  .bean-2 { bottom: 25%; right: 70%; animation-delay: 3s; }
  .bean-3 { top: 70%; left: 10%; animation-delay: 6s; }

  /* Coffee card styling */
  .coffee-card {
    background: linear-gradient(135deg, var(--coffee-800) 0%, var(--coffee-700) 100%);
    border-radius: 20px;
    padding: 32px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(212, 148, 58, 0.2);
    position: relative;
    overflow: hidden;
  }

  .coffee-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 30% 30%, rgba(212, 148, 58, 0.05) 0%, transparent 50%);
    pointer-events: none;
  }

  .special-card {
    background: linear-gradient(135deg, var(--pencil) 0%, #b8811a 100%);
    border: 2px solid rgba(255, 255, 255, 0.1);
    animation: gentle-glow 3s ease-in-out infinite;
  }

  @keyframes gentle-glow {
    0%, 100% { box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3), 0 0 20px rgba(212, 148, 58, 0.2); }
    50% { box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4), 0 0 30px rgba(212, 148, 58, 0.4); }
  }

  .card-header {
    text-align: center;
    margin-bottom: 24px;
  }

  .form-icon {
    width: 50px;
    height: 50px;
    background: var(--pencil);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 16px;
    color: white;
    box-shadow: 0 8px 20px rgba(212, 148, 58, 0.3);
  }

  .coffee-cup-floating {
    animation: float-cup 3s ease-in-out infinite;
  }

  @keyframes float-cup {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }

  /* Form styling */
  .form-group {
    position: relative;
  }

  .form-label {
    display: block;
    font-size: 0.9rem;
    font-weight: 600;
    margin-bottom: 8px;
    color: var(--cream);
  }

  .input-container {
    position: relative;
  }

  .form-input {
    width: 100%;
    padding: 16px 20px 16px 50px;
    border-radius: 12px;
    border: 2px solid rgba(212, 148, 58, 0.2);
    background: rgba(0, 0, 0, 0.1);
    color: var(--cream);
    font-size: 1rem;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
  }

  .form-input:focus {
    outline: none;
    border-color: var(--pencil);
    box-shadow: 0 0 0 3px rgba(212, 148, 58, 0.1);
    background: rgba(0, 0, 0, 0.15);
  }

  .form-input::placeholder {
    color: rgba(248, 244, 240, 0.6);
  }

  .form-textarea {
    resize: vertical;
    min-height: 120px;
  }

  .input-icon {
    position: absolute;
    left: 16px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--pencil);
    pointer-events: none;
  }

  .textarea-icon {
    top: 20px;
    transform: none;
  }

  /* Submit button */
  .submit-btn {
    width: 100%;
    padding: 16px 24px;
    background: linear-gradient(135deg, var(--pencil) 0%, #b8811a 100%);
    border: none;
    border-radius: 12px;
    color: white;
    font-weight: 600;
    font-size: 1.1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
  }

  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(212, 148, 58, 0.4);
  }

  .submit-btn:active {
    transform: translateY(0);
  }

  /* Contact items */
  .contact-item {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px;
    background: rgba(0, 0, 0, 0.1);
    border-radius: 12px;
    border: 1px solid rgba(212, 148, 58, 0.1);
    transition: all 0.3s ease;
  }

  .contact-item:hover {
    transform: translateY(-2px);
    background: rgba(0, 0, 0, 0.15);
    border-color: rgba(212, 148, 58, 0.3);
  }

  .contact-icon {
    width: 36px;
    height: 36px;
    background: var(--pencil);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    flex-shrink: 0;
  }

  .contact-link {
    color: var(--cream);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .contact-link:hover {
    color: var(--pencil);
  }

  /* Social media */
  .social-section {
    border-top: 1px solid rgba(212, 148, 58, 0.2);
    padding-top: 24px;
    margin-top: 24px;
  }

  .social-link {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    text-decoration: none;
  }

  .social-link.facebook { background: #1877f2; color: white; }
  .social-link.instagram { background: linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888); color: white; }
  .social-link.twitter { background: #1da1f2; color: white; }

  .social-link:hover {
    transform: translateY(-3px) scale(1.1);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  }

  /* Buy coffee section */
  .coffee-heart-container {
    text-align: center;
    margin-bottom: 20px;
  }

  .coffee-heart {
    display: inline-block;
    padding: 16px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    color: white;
    animation: pulse-heart 2s ease-in-out infinite;
  }

  @keyframes pulse-heart {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  .coffee-btn {
    display: block;
    width: 100%;
    padding: 16px 24px;
    background: white;
    color: var(--coffee-900);
    text-decoration: none;
    border-radius: 12px;
    font-weight: 700;
    font-size: 1.1rem;
    text-align: center;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .coffee-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3);
  }

  .btn-coffee-icon {
    font-size: 1.3rem;
    margin-right: 8px;
  }

  /* Animation classes */
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeInLeft {
    from { opacity: 0; transform: translateX(-30px); }
    to { opacity: 1; transform: translateX(0); }
  }

  @keyframes fadeInRight {
    from { opacity: 0; transform: translateX(30px); }
    to { opacity: 1; transform: translateX(0); }
  }

  @keyframes fadeInScale {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }

  .animate-fadeInUp { animation: fadeInUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards; }
  .animate-fadeInLeft { animation: fadeInLeft 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards; }
  .animate-fadeInRight { animation: fadeInRight 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards; }
  .animate-fadeInScale { animation: fadeInScale 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards; }

  /* Responsive design */
  @media (max-width: 768px) {
    .coffee-card { padding: 24px; }
    .form-input { padding: 14px 18px 14px 45px; }
    .input-icon { left: 14px; }
  }
</style>

<script>
  window.addEventListener('DOMContentLoaded', () => {
    const section = document.getElementById('contact');
    let animated = false;

    function animateContactSection() {
      if (animated) return;
      
      // Header animation
      const header = document.querySelector('.contact-header');
      if (header) {
        setTimeout(() => {
          header.style.opacity = '1';
          header.classList.add('animate-fadeInUp');
        }, 100);
      }
      
      // Form animation
      const form = document.querySelector('.contact-form');
      if (form) {
        setTimeout(() => {
          form.style.opacity = '1';
          form.classList.add('animate-fadeInLeft');
        }, 300);
      }
      
      // Form groups staggered animation
      const formGroups = document.querySelectorAll('.form-group');
      formGroups.forEach((group, index) => {
        setTimeout(() => {
          group.style.opacity = '1';
          group.classList.add('animate-fadeInUp');
        }, 500 + (index * 100));
      });
      
      // Contact info animation
      const contactInfo = document.querySelector('.contact-info');
      if (contactInfo) {
        setTimeout(() => {
          contactInfo.style.opacity = '1';
          contactInfo.classList.add('animate-fadeInRight');
        }, 600);
      }
      
      // Contact items staggered animation
      const contactItems = document.querySelectorAll('.contact-item');
      contactItems.forEach((item, index) => {
        setTimeout(() => {
          item.style.opacity = '1';
          item.classList.add('animate-fadeInUp');
        }, 800 + (index * 100));
      });
      
      // Social section animation
      const socialSection = document.querySelector('.social-section');
      if (socialSection) {
        setTimeout(() => {
          socialSection.style.opacity = '1';
          socialSection.classList.add('animate-fadeInScale');
        }, 1200);
      }
      
      // Buy coffee section animation
      const buyCoffeeSection = document.querySelector('.buy-coffee-section');
      if (buyCoffeeSection) {
        setTimeout(() => {
          buyCoffeeSection.style.opacity = '1';
          buyCoffeeSection.classList.add('animate-fadeInScale');
        }, 1000);
      }
      
      animated = true;
    }

    // Intersection Observer for scroll animation
    if (section) {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            animateContactSection();
            observer.disconnect();
          }
        });
      }, { threshold: 0.2 });
      
      observer.observe(section);
    }

    // Form interaction enhancements
    const form = document.getElementById('contact-form');
    const submitBtn = form?.querySelector('.submit-btn');
    
    if (form && submitBtn) {
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        
        // Show loading state
        const btnText = submitBtn.querySelector('.btn-text');
        const btnIcon = submitBtn.querySelector('.btn-icon');
        const btnLoading = submitBtn.querySelector('.btn-loading');
        
        if (btnText && btnIcon && btnLoading) {
          btnText.style.display = 'none';
          btnIcon.style.display = 'none';
          btnLoading.style.display = 'block';
          submitBtn.disabled = true;
        }
        
        // Simulate form submission (replace with actual form handling)
        await new Promise(resolve => setTimeout(resolve, 2000));
        
        // Reset button state
        if (btnText && btnIcon && btnLoading) {
          btnText.style.display = 'block';
          btnIcon.style.display = 'block';
          btnLoading.style.display = 'none';
          submitBtn.disabled = false;
          
          // Show success feedback
          submitBtn.style.background = 'linear-gradient(135deg, #22c55e 0%, #16a34a 100%)';
          btnText.textContent = 'Message Sent!';
          
          setTimeout(() => {
            submitBtn.style.background = 'linear-gradient(135deg, var(--pencil) 0%, #b8811a 100%)';
            btnText.textContent = 'Send Message';
          }, 3000);
        }
      });
    }

    // Input focus animations
    const inputs = document.querySelectorAll('.form-input');
    inputs.forEach(input => {
      input.addEventListener('focus', () => {
        const container = input.closest('.input-container');
        if (container) {
          container.style.transform = 'translateY(-2px)';
        }
      });
      
      input.addEventListener('blur', () => {
        const container = input.closest('.input-container');
        if (container) {
          container.style.transform = 'translateY(0)';
        }
      });
    });
  });
</script>